<div class="container">
  <div class="row">
    <div class="col-xl-12">


      <div style="margin: 50px 0;">
        <div style="margin-bottom: 20px;">
          <h2 class="text-center">ログイン</h2>
        </div>

          <div class="text-center">
            <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

              <div class="field", style="margin-bottom: 20px;">
                <%= f.label :email, "メールアドレス", style: "margin-right: 50px;" %>
                <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
              </div>

              <div class="field", style="margin-bottom: 20px;">
                <%= f.label :password, "パスワード", style: "margin-right: 82px;" %>
                <%= f.password_field :password, autocomplete: "current-password" %>
              </div>

              <% if devise_mapping.rememberable? %>
                <div class="field text-center">
                  <%= f.check_box :remember_me %>
                  <%= f.label :remember_me %>
                </div>
              <% end %>

              <div class="actions text-center">
                <%= f.submit "ログイン",  class: "btn btn-outline-success" %>
              </div>
            <% end %>
          </div>


        <div style="margin: 20px 0;", class="text-center">
          <p>会員登録がお済みでない方は<span><%= link_to "こちら", new_user_registration_path, class: "text-info" %></span>から新規登録をしてください</p>
        </div>

        <div class="border-top text-center", style="padding: 20px 0;">
          <%= link_to "ゲストログイン(閲覧用)", public_guest_sign_in_path, method: :post, class: "btn btn-outline-warning" %>
        </div>
      </div>


    </div>
  </div>
</div>

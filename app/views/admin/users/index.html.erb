<div class="container">
  <div class="row" style="margin: 50px 0;">
    <div class="col-xl-12">

      <div style="margin-bottom: 30px;">
        <h4 class="text-center">ユーザー一覧</h4>
      </div>

      <div class="d-flex justify-content-center">
        <table class="table">
          <thead>
            <tr>
              <th scope="col" class="text-nowrap font-weight-bold"><h6 style="margin-right: 20px">会員ID</h6></th>
              <th scope="col" class="text-nowrap font-weight-bold"><h6 style="margin-right: 90px">ユーザーネーム</h6></th>
              <th scope="col" class="text-nowrap font-weight-bold"><h6 style="margin-right: 120px">メールアドレス</h6></th>
              <th scope="col" class="text-nowrap font-weight-bold"><h6 style="margin-right: 50px">ステータス</h6></th>
          </tr>
          </thead>
          <tbody>
            <% @user.each do |user|%>
            <tr>
              <td><p class="pr-3"><%= user.id %></p></td>
              <td><p><%= link_to admin_user_path(user.id), class: "text-muted" do %>
                                  <%= user.name %>
                                <% end %></p></td>
              <td><p><%= user.email %></p></td>
              <td><p class="pr-3"><% if user.is_deleted %></p>
                                    <p class="text-muted">退会</p>
                                  <% else %>
                                    <p class="text-success">有効</p>
                                  <% end %></td>
            </tr>
            <% end %>
            <%= paginate @user %>
          </tbody>
        </table>
      </div>

    </div>
  </div>
</div>